#include <iostream>
using namespace std;

// 2번 3번 좌표 연습문제 통합작성
/*
class POINT 
{
	private:
		int x_point;
		int y_point;

	public:
		int getX() { return x_point; }
		int getY() { return y_point; }
		void setX(int a);
		void setY(int b);// set function = 범위내 검증과정
};

void POINT::setX(int a)
{
	if (a >= 0 && a <= 10)
		x_point = a;
	else
	{
		x_point = 0;
		cout << "0이상 10 이하의 숫자를 입력하세요.\n";
	}
}

void POINT::setY(int b)
{
	if (b >= 0 && b <= 10)
		y_point = b;
	else
	{
		y_point = 0;
		cout << "0이상 10 이하의 숫자를 입력하세요.\n";
	}
}

int main()
{
	POINT kano;
	int x=0;
	int y=0;

	cout << "X 좌표를 입력하십시오.\n";
	cin >> x;
	kano.setX(x);

	cout << "Y 좌표를 입력하십시오.\n";
	cin >> y;
	kano.setY(y);

	x = kano.getX();
	y = kano.getY();
	cout << "좌표는 (" << x << " , " << y << ") 입니다.\n";

	return 0;
}
*/
